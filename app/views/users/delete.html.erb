<%= form_tag("/users/#{@user.id}", method: :delete) do  %>
  <p>Are you sure you want to delete this user?</p>
    <button type="submit" class="btn waves-effect waves-light">Yes</button>
  <a href="/users/<%= @user.id %>" class="btn waves-effect waves-light">No, take me back!</a>
<% end %>
